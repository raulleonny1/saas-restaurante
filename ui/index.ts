/**
 * SmartServe UI — Atomic Design library.
 *
 * Import from `@/ui` (stable public API) or from layer paths:
 * `@/ui/atoms`, `@/ui/molecules`, `@/ui/organisms`.
 *
 * All components use CSS variables from `app/globals.css` and support
 * light/dark via `next-themes` (`class="dark"` on `<html>`).
 */

export * from "./atoms";
export * from "./molecules";
export * from "./organisms";
