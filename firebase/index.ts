/**
 * @deprecated Import from `@/lib/firebase` instead.
 * This folder name conflicts with root `firebase.json` under Turbopack.
 */
export {
  getDb,
  getFirebaseApp,
  getFirebaseAuth,
  getFirebaseStorage,
  isFirebaseConfigured,
  paths,
} from "@/lib/firebase";
